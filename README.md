# bear-MD
A "tiny" 9kb Markdown JS ES6+ parser with Async/Await and const / let with built in support for external links using the èxtl` attribute, :: for span tags and ::: for div tags with the option to use `-` to assign a CSS class.
